package com.android.banquetmanager.data.repository

import com.android.banquetmanager.data.model.Event

interface UserRepository {
//    suspend fun setUserPermissions(uid: String, permissions: Map<String, Boolean>): List<Event>
//    suspend fun getUserPermissions(uid: String): List<Event>
}